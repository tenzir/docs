// FIXME: remove the explicit context creation. We only have to do this here
// because the test framework is not installing the package at the node, but
// rather injecting --package-dirs with the tenzir binary (which ignores
// contexts outside of a node runtime).
context::create_lookup_table "sslbl"