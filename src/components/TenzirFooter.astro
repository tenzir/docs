---
import { Icon } from '@astrojs/starlight/components';
import { pathWithBase } from '@utils/base';

const currentYear = new Date().getFullYear();

type FooterLink = {
  text: string;
  href: string;
  icon?: string;
};

// Helper function to check if a link is external
const isExternalLink = (href: string) => {
  return href.startsWith('http://') || href.startsWith('https://') || href == '/discord';
};

// Resolve href with base path for local links
const resolveHref = (href: string) => {
  return isExternalLink(href) ? href : pathWithBase(href);
};

const footerSections: Record<string, FooterLink[]> = {
  Product: [
    { text: 'Overview', href: 'https://tenzir.com' },
    { text: 'Integrations', href: 'https://tenzir.com/integrations' },
  ],
  Resources: [
    { text: 'Blog', href: 'https://tenzir.com/blog' },
    { text: 'Events', href: 'https://tenzir.com/events' },
    { text: 'Sheets & Briefs', href: 'https://tenzir.com/resources' },
    { text: 'Documentation', href: '/' },
    { text: 'Changelog', href: '/changelog' },
  ],
  Company: [
    { text: 'About Tenzir', href: 'https://tenzir.com/company/about' },
    { text: 'Partners', href: 'https://tenzir.com/company/partners' },
    { text: 'Leadership', href: 'https://tenzir.com/company/leadership' },
    { text: 'Founding Story', href: 'https://tenzir.com/company/founding-story' },
    { text: 'Press Releases', href: 'https://tenzir.com/company/press' },
    { text: 'Contact', href: 'https://tenzir.com/contact' },
  ],
  Social: [
    { text: 'LinkedIn', href: 'https://linkedin.com/company/tenzir', icon: 'linkedin' },
    { text: 'GitHub', href: 'https://github.com/tenzir', icon: 'github' },
    { text: 'Discord', href: '/discord', icon: 'discord' },
    { text: 'Bluesky', href: 'https://bsky.app/profile/tenzir.bsky.social', icon: 'blueSky' },
    { text: 'X', href: 'https://x.com/tenzir_company', icon: 'x.com' },
  ],
};

const legalLinks = [
  { text: 'Legal Notice', href: 'https://tenzir.com/legal' },
  { text: 'Terms and Conditions', href: 'https://tenzir.com/terms' },
  { text: 'Privacy Statement', href: 'https://tenzir.com/privacy' },
];
---

<footer class="tenzir-footer">
  <div class="footer-content">
    <div class="footer-sections">
      {Object.entries(footerSections).map(([title, links]) => (
        <div class="footer-section">
          <h3 class:list={[
            "footer-section-title",
            title === "Social" && "has-icons"
          ]}>{title}</h3>
          <ul class="footer-links">
            {links.map((link) => (
              <li>
                <a
                  href={resolveHref(link.href)}
                  class="footer-link"
                  {...(isExternalLink(link.href) && {
                    target: '_blank',
                    rel: 'noopener noreferrer'
                  })}
                >
                  {link.icon && <Icon name={link.icon} size="1rem" />}
                  {link.text}
                </a>
              </li>
            ))}
          </ul>
        </div>
      ))}
    </div>

    <div class="footer-bottom">
      <div class="footer-copyright">
        © {currentYear} Tenzir GmbH. All rights reserved.
      </div>
      <div class="footer-legal">
        {legalLinks.map((link, index) => (
          <>
            <a
              href={link.href}
              class="footer-legal-link"
              {...(isExternalLink(link.href) && {
                target: '_blank',
                rel: 'noopener noreferrer'
              })}
            >
              {link.text}
            </a>
            {index < legalLinks.length - 1 && <span class="footer-separator">•</span>}
          </>
        ))}
      </div>
    </div>
  </div>
</footer>

<style>
  .tenzir-footer {
    background-color: var(--sl-color-bg);
    padding: var(--tnz-space-12) 0 var(--tnz-space-6);
    width: 100%;
    position: relative;
    z-index: 10;
  }

  .footer-content {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 var(--tnz-space-8);
    padding-top: var(--tnz-space-10);
    position: relative;
  }

  .footer-content::before {
    content: "";
    position: absolute;
    top: 0;
    left: var(--tnz-space-8);
    right: var(--tnz-space-8);
    height: 1px;
    background-color: var(--tnz-neutral-700);
  }

  :root[data-theme='light'] .footer-content::before {
    background-color: var(--tnz-neutral-200);
  }

  .footer-sections {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--tnz-space-10);
    margin-bottom: var(--tnz-space-10);
  }

  .footer-section-title {
    font-size: var(--tnz-text-xs);
    font-weight: var(--tnz-font-semibold);
    color: var(--tnz-neutral-400);
    letter-spacing: 0.05em;
    text-transform: uppercase;
    margin: 0 0 var(--tnz-space-3) 0;
  }

  :root[data-theme='light'] .footer-section-title {
    color: var(--tnz-neutral-500);
  }

  .footer-section-title.has-icons {
    /* No special styling needed */
  }

  .footer-links {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: var(--tnz-space-0-5);
  }

  .footer-links li {
    margin-bottom: 0;
  }

  .footer-link {
    color: var(--tnz-neutral-300);
    text-decoration: none;
    font-size: var(--tnz-text-sm);
    font-weight: var(--tnz-font-regular);
    line-height: var(--tnz-leading-sm);
    display: inline-flex;
    align-items: center;
    gap: var(--tnz-space-2);
    padding: var(--tnz-space-1) 0;
    border-bottom: 1px solid transparent;
    transition: color var(--tnz-transition-base), border-color var(--tnz-transition-base);
  }

  .footer-link:hover {
    color: var(--sl-color-white);
    border-bottom-color: var(--tnz-neutral-500);
  }

  :root[data-theme='light'] .footer-link {
    color: var(--tnz-neutral-600);
  }

  :root[data-theme='light'] .footer-link:hover {
    color: var(--tnz-neutral-800);
    border-bottom-color: var(--tnz-neutral-400);
  }

  .footer-bottom {
    display: flex;
    flex-direction: column;
    gap: var(--tnz-space-3);
    padding-top: var(--tnz-space-6);
    border-top: 1px solid var(--tnz-neutral-700);
    text-align: center;
  }

  :root[data-theme='light'] .footer-bottom {
    border-top-color: var(--tnz-neutral-200);
  }

  .footer-copyright {
    color: var(--tnz-neutral-500);
    font-size: var(--tnz-text-xs);
  }

  :root[data-theme='light'] .footer-copyright {
    color: var(--tnz-neutral-500);
  }

  .footer-legal {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: var(--tnz-space-4);
    flex-wrap: wrap;
  }

  .footer-legal-link {
    color: var(--tnz-neutral-400);
    text-decoration: none;
    font-size: var(--tnz-text-xs);
    display: inline-flex;
    transition: color var(--tnz-transition-base);
  }

  .footer-legal-link:hover {
    color: var(--sl-color-white);
  }

  :root[data-theme='light'] .footer-legal-link {
    color: var(--tnz-neutral-500);
  }

  :root[data-theme='light'] .footer-legal-link:hover {
    color: var(--tnz-neutral-800);
  }

  .footer-separator {
    color: var(--tnz-neutral-600);
    font-size: var(--tnz-text-xs);
  }

  :root[data-theme='light'] .footer-separator {
    color: var(--tnz-neutral-300);
  }

  @media (min-width: 50rem) {
    .footer-bottom {
      flex-direction: row;
      justify-content: space-between;
      align-items: center;
      text-align: left;
    }

    .footer-legal {
      justify-content: flex-end;
    }
  }

  @media (max-width: 72rem) {
    .footer-sections {
      grid-template-columns: repeat(2, 1fr);
      gap: var(--tnz-space-8) var(--tnz-space-6);
    }
  }

  @media (max-width: 50rem) {
    .footer-sections {
      grid-template-columns: 1fr;
      gap: var(--tnz-space-6);
    }

    .tenzir-footer {
      padding: var(--tnz-space-8) 0 var(--tnz-space-6);
    }

    .footer-content {
      padding: 0 var(--tnz-space-4);
    }
  }
</style>
