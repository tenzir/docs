---
title: Python API
description: Reference for the classes and helpers exported by the tenzir Python package.
---

import { CardGrid } from '@astrojs/starlight/components';
import ReferenceCard from '../../../../components/ReferenceCard.astro';

The `tenzir` Python package exposes helpers to run local pipelines with the
bundled `tenzir` binary and to post-process Arrow results. Import these
interfaces directly from `tenzir`; the package re-exports everything from the
internal `tenzir.tenzir.tenzir` and `tenzir.tenzir.convert` modules.

## API reference

<CardGrid>

<ReferenceCard
  title="Pipeline execution"
  description="Create and run Tenzir pipelines from Python with `PipelineSpec`, `run_pipeline()`, and execution helpers."
  href="/reference/python-api/pipeline-execution">

```python
result = run_pipeline_sync("from 'data.json'")
```

</ReferenceCard>

<ReferenceCard
  title="I/O configuration"
  description="Configure pipeline input and output with `PipelineIO` and its adapters for Arrow, JSON, and bytes."
  href="/reference/python-api/io-configuration">

```python
io = PipelineIO.arrow(input=table)
```

</ReferenceCard>

<ReferenceCard
  title="Data types"
  description="Work with table slices, Arrow batches, and structured event data using `PyArrowTableSlice` and `TenzirRow`."
  href="/reference/python-api/data-types">

```python
batch = slice._batch
```

</ReferenceCard>

<ReferenceCard
  title="Conversion utilities"
  description="Convert between Arrow and JSON formats with `to_pyarrow()`, `collect_pyarrow()`, and `to_json_rows()`."
  href="/reference/python-api/conversion-utilities">

```python
tables = await collect_pyarrow(stream)
```

</ReferenceCard>

<ReferenceCard
  title="Error handling"
  description="Handle pipeline failures with `PipelineError` exceptions that expose exit codes and diagnostic information."
  href="/reference/python-api/error-handling">

```python
except PipelineError as e:
    print(e.stderr)
```

</ReferenceCard>

</CardGrid>
