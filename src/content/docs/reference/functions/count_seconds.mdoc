---
title: count_seconds
---

Counts the number of `seconds` in a duration.

```tql
count_seconds(x:duration) -> float
```

{% partial file="../../../../partials/functions/_count_duration.mdoc" variables={ name: "seconds", unit: "s" } /%}
