---
title: count_seconds
category: Time & Date
example: 'count_seconds(x:duration) -> float'
---

Counts the number of `seconds` in a duration.

```tql
count_seconds(x:duration) -> float
```

import Body from '../../.../../../../partials/functions/CountDuration.mdx';

<Body name = "seconds" unit = "s" />
