---
title: count_weeks
---

Counts the number of `weeks` in a duration.

```tql
count_weeks(x:duration) -> float
```

{% partial file="../../../../partials/functions/_count_duration.mdoc" variables={ name: "weeks", unit: "w" } /%}
